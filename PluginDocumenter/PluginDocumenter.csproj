<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFramework>net48</TargetFramework>
    <OutputType>Library</OutputType>
    <AssemblyName>PluginDocumenter</AssemblyName>
    <RootNamespace>PluginDocumenter</RootNamespace>
    <Version>1.0.0</Version>
    <UseWindowsForms>true</UseWindowsForms>
    <Company>Comentality</Company>
    <Authors>Comentality</Authors>
    <Description>Document Dataverse plugin step registrations directly in your C# source, as Xrm Tools compatible [Plugin], [Step] and [Image] attributes.</Description>
    <GeneratePackageOnBuild>false</GeneratePackageOnBuild>
    <NuspecFile>PluginDocumenter.nuspec</NuspecFile>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="XrmToolBoxPackage" Version="1.2025.7.71">
      <PrivateAssets>all</PrivateAssets>
    </PackageReference>
  </ItemGroup>

  <ItemGroup>
    <!-- Single source of truth: the same file the tool writes into user projects. -->
    <EmbeddedResource Include="..\assets\XrmToolsMetaAttributes.cs">
      <LogicalName>PluginDocumenter.XrmToolsMetaAttributes.cs</LogicalName>
    </EmbeddedResource>
  </ItemGroup>
</Project>
