bin/
obj/
.vs/
*.user
*.suo
.claude/
.nuget-apikey
artifacts/
# The documenter leaves one of these beside every file it rewrites, including the fixtures.
*.bak
# The private XrmToolBox instance xtb.ps1 builds, including its connection and its token cache.
tests/.xtb/
# Layout screenshots from ui.ps1, rewritten on every run.
tests/.ui/
